{
  "name": "dynamic-popup",
  "version": "1.2.2",
  "author": "M. Enes Ertarhanacı",
  "description": "DynamicPopup relocates itself depending on the viewport's bounds.",
  "repository": {
    "type" : "git",
    "url" : "https://github.com/Splash-Inc/dynamic-popup.git"
  },
  "main": "dynamic-popup.min.js",
  "scripts": {
    "watch": "watch 'npm run build' ./src",
    "build": "npm run babel && npm run uglify",
    "babel": "babel --no-comments --compact=true ./src/src.js -o dynamic-popup.min.js",
    "uglify": "uglify -s dynamic-popup.min.js -o dynamic-popup.min.js"
  },
  "keywords": [
    "popup",
    "tooltip"
  ],
  "author": "",
  "license": "ISC"
}
