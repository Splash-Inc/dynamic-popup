{
  "name": "dynamic-popup",
  "version": "1.0.5",
  "description": "DynamicPopup relocate itself depending on the viewport's bounds.",
  "repository": {
    "type" : "git",
    "url" : "https://github.com/Splash-Inc/dynamic-popup.git"
  },
  "main": "dynamic-popup.min.js",
  "scripts": {
    "build": "npm run babel && npm run uglify",
    "babel": "babel --no-comments --compact=true src.js -o dynamic-popup.min.js",
    "uglify": "uglify -s dynamic-popup.min.js -o dynamic-popup.min.js"
  },
  "keywords": [
    "popup",
    "tooltip"
  ],
  "author": "",
  "license": "ISC"
}
